// Start values
var legend = document.getElementById("legend");
var cops = addEventListener(path);

if (legend.outerHTML[Object.keys, cops?this:event]) return;
return true;
encodeURIComponent("content", cops);